# AI 家教 —— 技术架构文档

> 版本：v1.0 | 日期：2026-06-05 | 配套需求文档 v3.0

---

## 一、架构总览

### 1.1 一句话架构

```
你电脑上的 Python 脚本 → 调 LLM → 生成自包含 HTML → 上传阿里云 OSS → 生成带签名的临时链接 → 发链接给闲鱼用户
```

### 1.2 系统边界

```
┌─────────────────────────────────────────────────────────┐
│                      你的电脑                            │
│                                                         │
│  ┌──────────────────────────────────────────────────┐  │
│  │                  generate.py                      │  │
│  │                                                   │  │
│  │  ① 解析资料                                       │  │
│  │     用户PPT/PDF/图片 → 提取知识点+考点              │  │
│  │                                                   │  │
│  │  ② 内容生成                                       │  │
│  │     LLM生成知识树 + 题库 + 解析 → 结构化JSON       │  │
│  │                                                   │  │
│  │  ③ 缓存管理                                       │  │
│  │     本地SQLite/JSON → 同教材复用 → 零成本          │  │
│  │                                                   │  │
│  │  ④ HTML打包                                       │  │
│  │     注入模板 → 自包含HTML文件                      │  │
│  │                                                   │  │
│  │  ⑤ 部署                                          │  │
│  │     上传阿里云OSS → 签名URL → 返回链接              │  │
│  └──────────────────────────────────────────────────┘  │
│                         │                                │
└─────────────────────────┼────────────────────────────────┘
                          │
              ┌───────────┴───────────┐
              ▼                       ▼
     ┌──────────────┐        ┌──────────────┐
     │  DeepSeek API │        │ 阿里云 OSS    │
     │  (LLM 调用)   │        │ (私有bucket)   │
     └──────────────┘        └──────┬───────┘
                                    │
                                    ▼
                           ┌──────────────┐
                           │   学生浏览器   │
                           │  (打开链接)    │
                           └──────────────┘
```

### 1.3 核心设计原则

| 原则 | 说明 |
|------|------|
| **零运行时依赖** | 生成的 HTML 是自包含的，不依赖任何后端服务 |
| **本地优先** | 所有计算在本地完成，不部署服务器 |
| **缓存复用** | 同一门课生成一次，后续订单直接复用 |
| **内容溯源** | HTML 页面标注每个知识点的来源，建立用户信任 |

---

## 二、技术栈

### 2.1 本地开发环境

| 技术 | 版本 | 用途 |
|------|------|------|
| **Python** | 3.12+ | 脚本主语言 |
| **Click** | 8.x | 命令行参数解析 |
| **Jinja2** | 3.x | HTML 模板渲染 |
| **SQLite** | 内置 | Token 与订单管理 |
| **python-pptx** | 1.x | PPT 文件解析 |
| **PyMuPDF (fitz)** | 1.24+ | PDF 文件解析 |
| **PaddleOCR** | 2.9+ | 图片/手写文字识别 |
| **httpx** | 0.28+ | LLM API 调用（异步） |
| **python-dotenv** | 1.x | 环境变量管理 |

### 2.2 LLM 服务

| 服务 | 用途 | 选型理由 |
|------|------|---------|
| **DeepSeek-V3** | 主力：题库生成、知识树、解析 | 便宜（2元/百万token输入），中文好 |
| **GPT-4o-mini** | 备选：复杂解析补充 | 质量高，成本可接受 |
| **BGE-M3（本地）** | 教材缓存去重匹配 | 免费，中文向量检索 |

### 2.3 HTML 输出端

| 技术 | 用途 |
|------|------|
| **MathJax** | 数学公式渲染（CDN 引入） |
| **原生 JS** | 刷题交互、判分、错题本、Token 校验 |
| **CSS 变量** | 纸质感主题（参考现有 ExamPass 样式） |
| **localStorage** | 用户答题记录、错题本 |

---

## 三、项目结构

```
ai-teacher/
├── generate.py              # 主入口脚本
├── config.py                # 配置（API Key、路径、默认参数）
├── deploy.py                # 上传阿里云 OSS，生成签名 URL
├── .env                     # LLM API Key（不提交git）
├── .gitignore
│
├── src/
│   ├── parser/              # 资料解析模块
│   │   ├── __init__.py
│   │   ├── ppt_parser.py    # PPT 文字提取
│   │   ├── pdf_parser.py    # PDF 文字提取
│   │   └── ocr.py           # 图片 OCR
│   │
│   ├── llm/                 # LLM 调用模块
│   │   ├── __init__.py
│   │   ├── client.py        # DeepSeek/OpenAI API 封装
│   │   ├── prompts.py       # Prompt 模板管理
│   │   ├── knowledge_tree.py # 知识树生成
│   │   ├── question_bank.py  # 题库生成
│   │   └── analysis.py      # 资料分析（考点提取）
│   │
│   ├── builder/             # HTML 构建模块
│   │   ├── __init__.py
│   │   ├── renderer.py      # Jinja2 模板渲染
│   │   ├── packer.py        # 数据注入 + 打包
│   │   └── validator.py     # 质量自检
│   │
│   ├── cache/               # 缓存模块
│   │   ├── __init__.py
│   │   ├── course_cache.py  # 教材/课程缓存
│   │   └── token_db.py      # SQLite Token 管理
│   │
│   └── deploy/              # 部署模块
│       ├── __init__.py
│       └── oss_upload.py    # OSS 上传 + 签名 URL 生成
│
├── templates/               # HTML 模板
│   ├── base.html            # 主模板骨架
│   ├── css/
│   │   └── style.css        # 全部样式（内嵌用）
│   ├── js/
│   │   ├── app.js           # 应用入口 + 路由
│   │   ├── quiz.js          # 刷题引擎
│   │   ├── wrongbook.js     # 错题本
│   │   ├── exam.js          # 模拟考试
│   │   ├── knowledge-tree.js # 知识树渲染
│   │   ├── charts.js        # 雷达图（Canvas）
│   │   └── auth.js          # Token 校验 + Cookie 绑定
│   └── components/
│       ├── knowledge-tab.html
│       ├── quiz-tab.html
│       ├── wrongbook-tab.html
│       ├── exam-tab.html
│       └── source-panel.html  # 资料溯源面板
│
├── cache/                   # 运行时缓存目录
│   └── courses/             # 已生成课程的缓存（JSON）
│       └── 同济高数第七版上册.json
│
├── output/                  # 生成的 HTML 文件
│   └── a1b2c3d4.html
│
├── data/
│   └── tokens.db            # SQLite（Token 记录）
│
└── docs/                    # 文档
    └── 需求文档_闲鱼+Token链接.md
```

---

## 四、核心模块设计

### 4.1 generate.py —— 主入口

```python
"""
用法：

  # 完整参数（PPT + 历年题 + 教材）
  python generate.py \
      --school "浙江大学" \
      --course "高等数学（上）" \
      --materials "PPT/ch1.pptx,PPT/ch2.pptx,去年期末.pdf" \
      --textbook "同济大学《高等数学》第七版 上册" \
      --days 30 \
      --user "闲鱼用户名"

  # 仅教材
  python generate.py \
      --school "浙江大学" \
      --course "高等数学（上）" \
      --textbook "同济大学《高等数学》第七版 上册" \
      --days 1 \
      --user "闲鱼用户名"

  # 仅课程名（兜底）
  python generate.py \
      --course "高等数学（上）" \
      --days 1 \
      --user "闲鱼用户名"
"""

import click
from src.parser import parse_materials
from src.llm import generate_content
from src.builder import build_html
from src.cache import check_cache, save_cache
from src.deploy import upload_to_oss

# ... (同上，只改最后一行)

    # 6. 上传 OSS + 生成签名 URL
    url = upload_to_oss(html_path, expire_days=days)

    print(f"✅ 生成完成！")
    print(f"   链接：{url}")
    print(f"   有效期：{days}天（链接到期后自动失效，OSS 服务端强制）")
    print(f"   LLM成本：~{content.cost}元")
```

### 4.2 资料解析模块 (src/parser/)

```python
# 资料解析的数据结构
@dataclass
class ParsedMaterial:
    points: list[KnowledgePoint]     # 提取的知识点
    examples: list[Question]         # 提取的例题/历年题
    exam_patterns: ExamPattern       # 题型分布规律
    coverage_ratio: float            # 覆盖课程的比例

@dataclass
class KnowledgePoint:
    name: str                        # 如 "洛必达法则"
    chapter: str                     # 所属章节
    source: str                      # 来源标记，如 "PPT第3页"
    importance: str                  # "必考" / "重点" / "了解"
    examples_count: int              # PPT中该知识点的例题数

@dataclass
class ExamPattern:
    question_types: dict             # {"选择": 20%, "填空": 15%, "计算": 40%, "证明": 25%}
    difficulty_dist: dict            # {"L1": 20%, "L2": 40%, "L3": 30%, "L4": 10%}
    high_freq_chapters: list[str]    # 高频章节列表
    total_score: int                 # 满分
```

**解析流程：**

```
PPT文件(.pptx)
  → python-pptx 逐页提取文字
  → LLM 识别：这是"知识点讲解"还是"例题"还是"重点标记"
  → 输出 KnowledgePoint 列表

PDF文件（历年题/重点)
  → PyMuPDF 提取文字（电子版）或 PaddleOCR（扫描版）
  → LLM 逐题结构化：题干→题型→章节→分值
  → 汇总分析：题型分布 + 高频考点 + 难度档位
  → 输出 ExamPattern

图片文件（目录照片/手写重点)
  → PaddleOCR 提取文字
  → LLM 结构化整理
```

### 4.3 LLM 调用模块 (src/llm/)

```python
# LLM 客户端封装
class LLMClient:
    """统一的 LLM 调用接口"""

    def __init__(self, provider="deepseek"):
        self.client = AsyncOpenAI(
            api_key=os.getenv("DEEPSEEK_API_KEY"),
            base_url="https://api.deepseek.com/v1"
        )
        self.model = "deepseek-chat"

    async def chat(self, system_prompt: str, user_prompt: str,
                   response_format: str = "json") -> dict:
        """发送请求，返回结构化 JSON"""
        ...

    async def chat_stream(self, ...):
        """流式调用（长文本生成用）"""
        ...
```

**Prompt 模板管理 (prompts.py)：**

```python
# 所有 Prompt 集中管理，便于迭代优化

PROMPT_ANALYZE_PPT = """
你是一位大学课程分析专家。以下是从老师PPT中提取的文字内容。
请识别：
1. 所有知识点（章节、名称、是否为老师强调的"重点"/"考点"）
2. 所有例题（题目内容、所属知识点、解题思路）
3. 题型分布规律（老师喜欢出什么类型的题）

PPT内容：
{ppt_text}

请以JSON格式输出...
"""

PROMPT_ANALYZE_EXAM = """
以下是一份历年期末考试题。请逐题结构化，并综合分析：
1. 每道题的：章节、知识点、题型、难度(1-4)、分值
2. 章节分值分布（哪些章节分值高 = 重点章节）
3. 题型分布规律
4. 重复出现的考点（高频考点）
...

历年题内容：
{exam_text}
"""

PROMPT_GENERATE_KNOWLEDGE_TREE = """
请根据以下信息生成完整的知识树：
- 课程：{course}
- 学校：{school}
- 教材：{textbook}
- 用户资料提取的知识点：{user_points}

要求：
1. 用户资料中的知识点必须全部包含，标注 [来源：用户资料]
2. 教材中用户资料未覆盖的章节作为补充，标注 [来源：教材补充]
3. 每个知识点标注考频等级（必考/高频/常规/了解）
...
"""

PROMPT_GENERATE_QUESTIONS = """
请为以下知识点生成练习题：

知识点：{knowledge_point}
来源：{source}
题型偏好：{exam_pattern}
难度分布：{difficulty_dist}
题量要求：{count} 道

要求：
1. 题型和难度与历年题保持一致
2. 选择题的干扰项要有迷惑性（合理混淆，非随机）
3. 每道题附带分步解析 + 易错提醒
4. 如果来源标记为"用户资料"，题目风格要贴近PPT中的例题
...
"""
```

**LLM 调用流程 (流水线)：**

```
Step 1: 分析资料（并行）
  ├── PPT分析 → 知识点 + 例题
  ├── 历年题分析 → 题型规律 + 高频考点
  └── 教材目录 → 章节结构

Step 2: 生成知识树（串行，依赖 Step 1）
  └── 合并所有输入 → 完整知识树 JSON

Step 3: 生成题库（并行，每个知识点独立）
  ├── 知识点1 → 10道题
  ├── 知识点2 → 10道题
  ├── ...
  └── 知识点N → 8道题（教材补充章节，题量减半）

Step 4: 质量自检（串行，依赖 Step 3）
  └── 覆盖率、难度对齐、解析完整性
```

### 4.4 HTML 构建模块 (src/builder/)

```python
# renderer.py —— 模板渲染
class HTMLRenderer:
    """将知识树 + 题库渲染为自包含 HTML"""

    def __init__(self, template_dir="templates/"):
        self.env = Environment(loader=FileSystemLoader(template_dir))

    def render(self, context: dict) -> str:
        """渲染完整 HTML"""

        # 1. 将 CSS 内嵌到 HTML
        context["inline_css"] = self._read_css()

        # 2. 将 JS 内嵌到 HTML（不依赖外部文件）
        context["inline_js"] = self._read_js()

        # 3. 知识树 JSON 直接注入 <script> 标签
        context["knowledge_tree_json"] = json.dumps(
            context["knowledge_tree"], ensure_ascii=False
        )

        # 4. 题库 JSON 注入
        context["question_bank_json"] = json.dumps(
            context["question_bank"], ensure_ascii=False
        )

        # 5. 配置注入（Token、有效期、来源面板数据）
        context["config_json"] = json.dumps({
            "token": context["token"],
            "school": context["school"],
            "course": context["course"],
            "textbook": context["textbook"],
            "expire_at": context["expire_at"],
            "user_name": context["user_name"],
            "source_panel": context["source_panel"],  # 溯源数据
        }, ensure_ascii=False)

        # 6. 渲染模板
        template = self.env.get_template("base.html")
        return template.render(**context)
```

**打包后的 HTML 结构：**

```html
<!DOCTYPE html>
<html lang="zh-CN">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>浙江大学 · 高等数学（上）· 期末定制版</title>

  <!-- MathJax CDN（唯一外部依赖） -->
  <script defer src="https://cdn.jsdelivr.net/npm/mathjax@3/es5/tex-chtml-full.js"></script>

  <!-- 全部 CSS 内嵌 -->
  <style>/* ~15KB CSS */</style>
</head>
<body>
  <!-- ====== 顶部信息栏 ====== -->
  <header>
    <div class="brand">AI 期末刷题</div>
    <div class="user-info">为【闲鱼用户 xxx】定制 · 有效期至 2026-07-05</div>
  </header>

  <!-- ====== 资料溯源面板 ====== -->
  <div class="source-panel">
    <details>
      <summary>📋 本题库生成依据（点击展开）</summary>
      ...
    </details>
  </div>

  <!-- ====== 导航 Tab ====== -->
  <nav class="tabs">
    <button class="tab active" data-tab="knowledge">📖 知识点</button>
    <button class="tab" data-tab="quiz">✏️ 刷题</button>
    <button class="tab" data-tab="wrongbook">📝 错题本</button>
    <button class="tab" data-tab="exam">📋 模拟考</button>
  </nav>

  <!-- ====== Tab 内容区 ====== -->
  <div id="tab-knowledge"><!-- 知识树面板 --></div>
  <div id="tab-quiz" style="display:none"><!-- 刷题面板 --></div>
  <div id="tab-wrongbook" style="display:none"><!-- 错题本面板 --></div>
  <div id="tab-exam" style="display:none"><!-- 模拟考面板 --></div>

  <!-- ====== 全部 JS 内嵌 ====== -->
  <script>
    // ── 数据注入 ──
    const KNOWLEDGE_TREE = { /* 知识树 JSON */ };
    const QUESTION_BANK = [ /* 题库 JSON */ ];
    const CONFIG = { /* Token、有效期、用户名、溯源数据 */ };

    // ── 前端逻辑（~30KB 压缩后 ~12KB）──
    // Token校验 + 到期检查
    // 知识树渲染
    // 刷题引擎（选题、判分、解析展示）
    // 错题本（localStorage）
    // 模拟考试（组卷、计时、出分）
    // 雷达图（Canvas手绘）
  </script>
</body>
</html>
```

### 4.5 缓存模块 (src/cache/)

```python
# course_cache.py —— 教材/课程缓存

"""
缓存策略：

缓存粒度：{school}_{course}_{textbook}
缓存内容：
  - knowledge_tree.json  （知识树）
  - question_bank.json   （题库，含解析）
  - meta.json            （生成时间、资料摘要、LLM成本）

缓存命中条件：
  - 同一学校 + 同一课程 + 同一教材 → 直接复用
  - 同一课程 + 同一教材，不同学校 → 提示用户"不同学校重点可能不同，是否要重新分析？"
  - 如果用户提供了新的PPT/历年题 → 不复用缓存，重新生成

缓存存储：
  cache/courses/{课程名}_{教材slug}.json
"""

def check_cache(school: str, course: str, textbook: str,
                has_new_materials: bool) -> dict | None:
    """检查缓存，返回知识树+题库 或 None"""
    if has_new_materials:
        return None  # 有新资料，不走缓存

    key = f"{course}_{_textbook_slug(textbook)}"
    path = f"cache/courses/{key}.json"

    if os.path.exists(path):
        with open(path, "r", encoding="utf-8") as f:
            cached = json.load(f)
        # 检查缓存时效（超过 6 个月建议刷新）
        if _is_fresh(cached["created_at"], days=180):
            return cached["knowledge_tree"], cached["question_bank"]
    return None
```

```python
# token_db.py —— Token 管理

"""
SQLite 表结构：

CREATE TABLE tokens (
    id          INTEGER PRIMARY KEY AUTOINCREMENT,
    token       TEXT UNIQUE NOT NULL,        -- 32位hex
    school      TEXT,
    course      TEXT NOT NULL,
    textbook    TEXT,
    materials   TEXT,                         -- 用户资料摘要
    cache_key   TEXT,                         -- 关联缓存
    days        INTEGER DEFAULT 30,           -- 有效期（天）
    user_name   TEXT,                         -- 闲鱼用户名
    price       REAL,                         -- 售价
    status      TEXT DEFAULT 'active',        -- active | expired | revoked
    created_at  TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    activated_at TIMESTAMP,                   -- 首次访问时间
    expire_at   TIMESTAMP,
    device_id   TEXT                          -- 绑定的设备ID
);

主要操作：
  - create_token()  → 生成新 Token，返回链接
  - validate_token(token) → 检查是否有效（状态+有效期）
  - activate_token(token, device_id) → 首次打开绑定设备
  - renew_token(token, days) → 续期
  - revoke_token(token) → 手动作废
  - list_tokens(user_name) → 查看某用户的所有订单
"""
```

### 4.6 部署模块 (src/deploy/)

#### 为什么不用 GitHub Pages

GitHub Pages 仓库是公开的，任何人拿到链接都能通过 F12 查看源码、绕过 Token 校验。**阿里云 OSS 签名 URL 在服务端校验过期时间，不可伪造。**

#### 阿里云 OSS 签名 URL 原理

```
你的操作：
  上传 HTML 到 OSS（bucket 设为私有，不公开读）
  用 AccessKey 生成一个带签名的临时访问链接

学生拿到的链接：
  https://xxx.oss-cn-hangzhou.aliyuncs.com/a1b2c3d4.html?
    Expires=1751234567&
    OSSAccessKeyId=xxx&
    Signature=yyy

关键特性：
  - Signature 是你用 AccessKey Secret 签发的，无人能伪造
  - Expires 到期后 OSS 服务端直接拒绝请求（404），前端绕过无效
  - 不需要你写任何后端代码，OSS 替你做了鉴权
  - 即使别人拿到链接，有效期过了也打不开
```

```python
# oss_upload.py

import oss2
from pathlib import Path

class OSSDeployer:
    """阿里云 OSS 部署 + 签名 URL 生成"""

    def __init__(self):
        self.auth = oss2.Auth(
            os.getenv("OSS_ACCESS_KEY_ID"),
            os.getenv("OSS_ACCESS_KEY_SECRET")
        )
        self.bucket = oss2.Bucket(
            self.auth,
            os.getenv("OSS_ENDPOINT", "oss-cn-hangzhou.aliyuncs.com"),
            os.getenv("OSS_BUCKET_NAME")
        )

    def upload(self, local_path: str, expire_days: int = 30) -> str:
        """上传文件并返回带签名的临时链接"""

        token = Path(local_path).stem  # a1b2c3d4

        # 1. 上传到 OSS（bucket 设为私有读写）
        object_name = f"quiz/{token}.html"
        self.bucket.put_object_from_file(object_name, local_path)

        # 2. 生成签名 URL（过期时间 = 用户购买的天数）
        expire_seconds = expire_days * 24 * 3600
        signed_url = self.bucket.sign_url(
            'GET', object_name, expire_seconds
        )

        return signed_url


def upload_to_oss(html_path: str, expire_days: int = 30) -> str:
    deployer = OSSDeployer()
    return deployer.upload(html_path, expire_days)
```

#### OSS 安全配置要点

| 配置项 | 设置 | 说明 |
|--------|------|------|
| Bucket 权限 | **私有** | 禁止公开读取 |
| AccessKey | RAM 子账号 | 不用主账号 AK，权限最小化（仅此 Bucket 的读写） |
| 防盗链 Referer | 留空 | 签名 URL 本身就是鉴权，不需要额外防盗链 |
| 签名有效期 | = 用户购买天数 | 到期自动失效，无需手动清理 |

#### 成本

```
OSS 存储：
  一个 HTML ~2MB，1000 个文件 ~2GB
  存储费：2GB × 0.12元/GB/月 ≈ 0.24 元/月

OSS 流量：
  一个学生每天刷题约 5 次页面加载
  单文件 2MB × 5 次 × 30 天 = 300MB/月/人
  外网流出：0.25元/GB
  100 个学生：100 × 0.3GB × 0.25 ≈ 7.5 元/月

总计：~10 元/月（100 学生规模）
      ~50 元/月（1000 学生规模）
```

---

## 五、前端 JS 逻辑设计

### 5.1 模块结构

```
app.js          → 入口：Tab 切换、页面初始化、Token 校验
quiz.js         → 刷题引擎：选题、选项渲染、提交判分、解析展示
wrongbook.js    → 错题本：localStorage 读写、错题重做、移除
exam.js         → 模拟考：随机组卷、倒计时、自动提交、出分报告
knowledge-tree.js → 知识树：递归渲染、展开折叠、颜色标记
charts.js       → 雷达图：Canvas 2D 手绘（不引入 Chart.js 以保持自包含）
auth.js         → Token 校验 + Cookie 绑定 + 到期倒计时
```

### 5.2 刷题引擎核心逻辑

```javascript
// quiz.js —— 核心数据结构

const QuizEngine = {
  currentMode: 'by_kp',       // 'by_kp' | 'random' | 'wrong_only'
  currentKP: null,            // 当前筛选的知识点
  currentIndex: 0,            // 当前题目索引
  filteredQuestions: [],      // 按筛选条件过滤后的题目列表
  answers: {},                // 用户答案 { qid: answer }

  // 选题
  selectQuestions(mode, kp) {
    if (mode === 'by_kp') {
      this.filteredQuestions = QUESTION_BANK.filter(q => q.knowledge_point === kp);
    } else if (mode === 'wrong_only') {
      const wrongIds = WrongBook.getAllIds();
      this.filteredQuestions = QUESTION_BANK.filter(q => wrongIds.includes(q.id));
    } else {
      this.filteredQuestions = [...QUESTION_BANK];
    }
    // 打乱顺序
    shuffle(this.filteredQuestions);
    this.currentIndex = 0;
    this.answers = {};
  },

  // 提交单题
  submitAnswer(qid, answer) {
    const q = QUESTION_BANK.find(q => q.id === qid);
    const isCorrect = this._checkAnswer(q, answer);
    this.answers[qid] = { answer, isCorrect, time: Date.now() };

    if (!isCorrect) {
      WrongBook.add(qid);  // 自动加入错题本
    } else {
      WrongBook.remove(qid); // 做对了就从错题本移除
    }

    return {
      isCorrect,
      correctAnswer: q.answer,
      explanation: q.explanation,
      pitfall: q.pitfall
    };
  },

  // 判分逻辑（参考 Java综合测试.html）
  _checkAnswer(q, userAnswer) {
    switch (q.type) {
      case 'choice':  // 单选
        return userAnswer === q.answer;
      case 'multi':   // 多选（少选得分，错选零分）
        return arraysEqual(userAnswer.sort(), q.answer.sort());
      case 'tf':      // 判断
        return userAnswer === q.answer;
      case 'fill':    // 填空（标准化比较，忽略空格和标点）
        return normText(userAnswer) === normText(q.answer[0]);
      default:
        return null;  // 主观题需人工判断
    }
  }
};
```

### 5.3 Token 校验与设备绑定

```javascript
// auth.js —— 首次打开时的校验流程

(function() {
  const TOKEN = CONFIG.token;
  const DEVICE_ID_KEY = 'ai_quiz_device_id';
  const ACTIVATED_KEY = 'ai_quiz_activated';

  // 1. 检查是否已被激活
  let deviceId = getCookie(DEVICE_ID_KEY);

  if (!deviceId) {
    // 首次打开 → 绑定设备
    deviceId = generateDeviceId();  // 随机32位 + 时间戳
    setCookie(DEVICE_ID_KEY, deviceId, 365);  // 存一年
    setCookie(ACTIVATED_KEY, Date.now(), 365);

    // 可选：上报激活（如果有极简 Serverless API）
    // fetch('https://your-api.com/activate', { method: 'POST', body: JSON.stringify({token: TOKEN, device: deviceId}) });
  } else {
    // 已有设备ID → 检查是否在当前 Token 的允许范围内
    // 如果 Token 被分享了，deviceId 不匹配 → 锁定页面
  }

  // 2. 检查有效期
  const expireAt = new Date(CONFIG.expire_at).getTime();
  const now = Date.now();

  if (now > expireAt) {
    showExpiredPage();
    return;
  }

  // 3. 到期前提醒
  const hoursLeft = (expireAt - now) / 3600000;
  if (hoursLeft < 48) {
    showRenewalBanner(hoursLeft);
  }

  // 4. 启动倒计时
  startCountdown(expireAt);
})();
```

### 5.4 错题本（localStorage）

```javascript
// wrongbook.js

const WrongBook = {
  STORAGE_KEY: 'ai_quiz_wrongbook',

  getAllIds() {
    const data = localStorage.getItem(this.STORAGE_KEY);
    return data ? JSON.parse(data) : [];
  },

  add(qid) {
    const ids = this.getAllIds();
    if (!ids.includes(qid)) {
      ids.push(qid);
      localStorage.setItem(this.STORAGE_KEY, JSON.stringify(ids));
    }
  },

  remove(qid) {
    const ids = this.getAllIds().filter(id => id !== qid);
    localStorage.setItem(this.STORAGE_KEY, JSON.stringify(ids));
  },

  getByKnowledgePoint() {
    // 按知识点分组返回错题
    const ids = this.getAllIds();
    const questions = ids.map(id => QUESTION_BANK.find(q => q.id === id));
    return groupBy(questions, 'knowledge_point');
  },

  clear() {
    localStorage.removeItem(this.STORAGE_KEY);
  }
};
```

---

## 六、数据流全貌

### 6.1 一次完整生成的数据流

```
用户输入（命令行参数）
  │
  ├── school, course, textbook, materials, days, user
  │
  ▼
┌─────────────────────────────────────────────┐
│ Step 1: 资料解析                             │
│                                             │
│ materials/                                  │
│   ├── ch1.pptx  →  python-pptx  →  纯文本    │
│   ├── ch2.pptx  →  python-pptx  →  纯文本    │
│   └── exam.pdf  →  PyMuPDF     →  纯文本    │
│                                             │
│  纯文本 → LLM分析 → ParsedMaterial           │
│    ├── points: [{name, chapter, source, ...}]│
│    ├── examples: [{q_text, kp, type, ...}]   │
│    └── exam_patterns: {types, difficulty}    │
└──────────────┬──────────────────────────────┘
               │
               ▼
┌─────────────────────────────────────────────┐
│ Step 2: 知识树生成                           │
│                                             │
│  LLM 输入：                                  │
│  - 用户资料提取的知识点                        │
│  - 教材目录（如有）                           │
│  - 课程公开大纲（MOOC/考研大纲）               │
│                                             │
│  LLM 输出：                                  │
│  KnowledgeTree JSON                         │
│    ├── chapters: [{title, sections: [...]}]  │
│    ├── 每个节点: {name, source, importance}   │
│    └── 来源标记: PPT/教材/大纲                 │
└──────────────┬──────────────────────────────┘
               │
               ▼
┌─────────────────────────────────────────────┐
│ Step 3: 题库生成（每个知识点并行）             │
│                                             │
│ 知识点1 → LLM → 10道题                       │
│   ├── 题型：按 ExamPattern 的分布             │
│   ├── 难度：按 ExamPattern 的分布             │
│   └── 每道题：题干+选项+答案+分步解析+易错提醒  │
│                                             │
│ 知识点2 → LLM → 10道题                       │
│ ...                                         │
│                                             │
│ 汇总 → QuestionBank JSON (150-200 道题)     │
└──────────────┬──────────────────────────────┘
               │
               ▼
┌─────────────────────────────────────────────┐
│ Step 4: 质量自检                             │
│                                             │
│  □ PPT知识点覆盖率 = 100%？                   │
│  □ 题型分布 ±10% 误差内？                     │
│  □ 每道题有分步解析？                         │
│  □ 高错题有易错提醒？                         │
│  □ 选择题干扰项合理？                         │
│                                             │
│  不通过 → 重试（最多2次）→ 仍不通过 → 标记    │
└──────────────┬──────────────────────────────┘
               │
               ▼
┌─────────────────────────────────────────────┐
│ Step 5: 缓存                                │
│                                             │
│  存入 cache/courses/{key}.json              │
│    ├── knowledge_tree                       │
│    ├── question_bank                        │
│    ├── meta: {created_at, llm_cost, ...}    │
│    └── 下次同教材直接复用                     │
└──────────────┬──────────────────────────────┘
               │
               ▼
┌─────────────────────────────────────────────┐
│ Step 6: HTML 打包                           │
│                                             │
│  Jinja2 渲染模板                             │
│    + knowledge_tree_json 注入               │
│    + question_bank_json 注入                │
│    + config_json 注入（Token/有效期/溯源）    │
│    + CSS 内嵌                               │
│    + JS 内嵌                                │
│                                             │
│  → output/a1b2c3d4.html (约 1-2MB)         │
└──────────────┬──────────────────────────────┘
               │
               ▼
┌─────────────────────────────────────────────┐
│ Step 7: 部署                                │
│                                             │
│  上传至阿里云 OSS（私有 bucket）               │
│  bucket.sign_url('GET', object, expire_seconds) │
│                                             │
│  → https://xxx.oss-cn-hangzhou.aliyuncs.com/ │
│    quiz/a1b2c3d4.html?Expires=...&Signature=... │
│    （签名不可伪造，到期服务端强制拒绝）         │
└─────────────────────────────────────────────┘
```

### 6.2 用户使用时的数据流

```
学生打开链接
  │
  ▼
浏览器加载 HTML（一次请求，全部数据已在文件里）
  │
  ▼
auth.js 执行 Token 校验
  ├── 检查有效期 → 过期则显示到期页面
  ├── 检查设备绑定 → 首次打开绑定 Cookie
  └── 通过 → 显示主界面
  │
  ▼
学生刷题
  ├── 选题 → quiz.js 从 QUESTION_BANK 筛选
  ├── 答题 → 前端判断对错
  ├── 看解析 → 预生成在 HTML 里的 explanation
  └── 错题 → 写入 localStorage
  │
  ▼
学生关闭浏览器 → 下次打开
  ├── Cookie 还在 → 设备验证通过
  ├── localStorage 还在 → 错题本/进度不丢
  └── 继续刷题
  │
  ▼
有效期到期
  └── auth.js 检测到过期 → 显示续费页面
```

---

## 七、本地开发 → 交付流程

### 7.1 初始搭建（一次性）

```
1. 安装 Python 依赖
   pip install click jinja2 python-pptx pymupdf paddleocr httpx python-dotenv

2. 注册 DeepSeek API，充值 100 元
   https://platform.deepseek.com

3. 注册阿里云 OSS，创建 Bucket（权限设为私有），获取 AccessKey
   https://oss.console.aliyun.com

4. 配置 .env
   DEEPSEEK_API_KEY=sk-xxx
   OSS_ACCESS_KEY_ID=LTAI5txxx
   OSS_ACCESS_KEY_SECRET=xxx
   OSS_ENDPOINT=oss-cn-hangzhou.aliyuncs.com
   OSS_BUCKET_NAME=ai-quiz-pages

5. 准备 HTML 模板（参考现有 第2章_知识清单.html 和 Java综合测试.html 的样式）
```

### 7.2 日常交付流程

```
用户闲鱼下单
  ↓
你收到消息：学校+课程+资料（可能有PPT/历年题/教材）
  ↓
打开终端，运行：
  python generate.py --school "浙大" --course "高数上" \
      --materials "用户PPT/高数复习.pptx,用户PPT/去年期末.pdf" \
      --textbook "同济高数第七版" --days 1 --user "闲鱼用户xxx"
  ↓
等待 1-3 分钟（LLM 生成中）
  ↓
输出链接 → 复制 → 发闲鱼消息给用户
  ↓
用户确认收到 → 订单完成
```

### 7.3 续费流程

```
用户闲鱼找你续费
  ↓
运行续费命令：
  python renew.py --token "a1b2c3d4" --add_days 30
  ↓
脚本：
  - 更新 tokens.db 中该 Token 的 expire_at
  - 重新上传 HTML（更新有效期）
  - 用户打开原链接 → 数据不丢 + 有效期延长
```

---

## 八、开发优先级与工时估算

### Phase 1：跑通最小闭环（5-7 天）

| 任务 | 工时 | 产出 |
|------|:---:|------|
| 项目骨架搭建 | 0.5天 | 目录结构、config、.env |
| HTML 模板改造 | 2天 | 基于现有两个 HTML，改造成 4 Tab 模板 |
| 题库数据手动注入 | 0.5天 | 手写 20 道题验证前端刷题逻辑 |
| generate.py 基础版 | 1天 | 命令行参数 + 调用 LLM 生成知识树 |
| 题库 LLM 生成 | 1天 | 单个知识点生成题目 + 注入模板 |
| OSS 部署 | 0.5天 | 上传 + 签名 URL → 链接可访问 |
| Token + Cookie 绑定 | 0.5天 | 首次激活 + 到期校验 |
| **小计** | **6天** | **可交付第一个用户** |

### Phase 2：资料解析 + 缓存（3-4 天）

| 任务 | 工时 | 产出 |
|------|:---:|------|
| PPT 解析 | 1天 | python-pptx + LLM 分析 |
| PDF/图片 OCR | 1天 | PyMuPDF + PaddleOCR |
| 教材目录识别 | 0.5天 | LLM 直接查 + OCR 备选 |
| 缓存系统 | 0.5天 | JSON 文件缓存 + 复用逻辑 |
| **小计** | **3天** | **支持用户上传资料定制** |

### Phase 3：体验打磨（2-3 天）

| 任务 | 工时 | 产出 |
|------|:---:|------|
| 质量自检 | 1天 | 自动检查 + 报告 |
| 资料溯源面板 | 0.5天 | HTML 中展示内容来源 |
| 移动端适配 | 0.5天 | 手机测试 + CSS 调整 |
| 到期续费提醒 | 0.5天 | 弹窗 + 引导 |
| **小计** | **2.5天** | **产品体验完整** |

> **总计工时：约 12 个工作日。一个人全职做，两周半可以跑通完整流程。**

---

## 九、技术风险与应对

| 风险 | 概率 | 应对 |
|------|:---:|------|
| **DeepSeek API 不稳定** | 中 | 备选通义千问/智谱 GLM，均为 OpenAI 兼容接口，改一行 base_url 即可切换 |
| **PaddleOCR 中文手写识别差** | 中 | 手写内容只做参考，让学生尽量发电子版；加入人工校对步骤 |
| **OSS 签名 URL 被恶意刷流量** | 低 | 链接有有效期且按量计费，单文件仅 2MB；可配 CDN 缓解 |
| **LLM 生成题目质量不稳定** | 中 | 质量自检自动拦截 + 重试；首批题目人工抽查 |
| **HTML 被 F12 查看源码复制** | 低 | Token 校验在前端无法完全防住；真正的壁垒是"内容足够便宜，不值得复制" |
| **闲鱼封号** | 低 | 虚拟服务类目正常经营即可；备 1-2 个备用号 |

---

## 十、关键文件清单

| 文件 | 职责 | 核心度 |
|------|------|:---:|
| `generate.py` | 主入口，编排全流程 | ⭐⭐⭐⭐⭐ |
| `src/llm/prompts.py` | 所有 Prompt 模板 | ⭐⭐⭐⭐⭐ |
| `src/llm/question_bank.py` | 题库生成逻辑 | ⭐⭐⭐⭐ |
| `src/parser/ppt_parser.py` | PPT 解析 | ⭐⭐⭐⭐ |
| `src/builder/renderer.py` | HTML 渲染打包 | ⭐⭐⭐⭐ |
| `templates/base.html` | HTML 模板骨架 | ⭐⭐⭐⭐ |
| `templates/js/quiz.js` | 刷题引擎 | ⭐⭐⭐⭐ |
| `src/cache/course_cache.py` | 缓存复用 | ⭐⭐⭐ |
| `src/cache/token_db.py` | Token 管理 | ⭐⭐⭐ |
| `src/deploy/oss_upload.py` | OSS 部署 + 签名 URL | ⭐⭐ |

---

> 本文档为初始技术架构，随开发推进持续更新。核心哲学：**用脚本代替服务器，用静态文件代替数据库，用 LLM 代替爬虫。**
